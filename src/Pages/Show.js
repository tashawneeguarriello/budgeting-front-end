import TransactionDetails from "../Components/TransactionDetails";
// import { useParams } from "react-router-dom";

function Show() {
  //const { index } = useParams();
  return (
    <div className="TransactionsD">
      <TransactionDetails />
    </div>
  );
}

export default Show;
